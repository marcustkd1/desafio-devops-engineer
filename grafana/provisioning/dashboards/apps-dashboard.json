{
  "title": "Aplicações - FastAPI & Go",
  "timezone": "browser",
  "schemaVersion": 36,
  "refresh": "5s",
  "panels": [
    {
      "type": "timeseries",
      "title": "Requisições por Segundo (Req/s)",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "targets": [
        {
          "expr": "sum by (handler) (rate(http_requests_total{job=\"python-app\"}[5m]))",
          "legendFormat": "Python - {{handler}}"
        },
        {
          "expr": "sum by (handler) (rate(go_http_requests_total{job=\"go-app\"}[5m]))",
          "legendFormat": "Go - {{handler}}"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Latência Média (Segundos)",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "targets": [
        {
          "expr": "sum by (handler) (rate(http_request_duration_seconds_sum{job=\"python-app\"}[5m])) / sum by (handler) (rate(http_request_duration_seconds_count{job=\"python-app\"}[5m]))",
          "legendFormat": "Python - {{handler}}"
        },
        {
          "expr": "sum by (handler) (rate(go_http_request_duration_seconds_sum{job=\"go-app\"}[5m])) / sum by (handler) (rate(go_http_request_duration_seconds_count{job=\"go-app\"}[5m]))",
          "legendFormat": "Go - {{handler}}"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Status Codes (Python FastAPI)",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "targets": [
        {
          "expr": "sum by (status) (rate(http_requests_total{job=\"python-app\"}[5m]))",
          "legendFormat": "HTTP {{status}}"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Status Codes (Go App)",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "targets": [
        {
          "expr": "sum by (status) (rate(go_http_requests_total{job=\"go-app\"}[5m]))",
          "legendFormat": "HTTP {{status}}"
        }
      ]
    }
  ]
}
